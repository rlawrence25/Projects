/**
 * A divide and conquer algorithms. Divides a given array in half, call itself 
 * for the two halves and then mergers the two sorted halves. There is a separte merge
 * function `merge(arr, l, m, r)` -> `arr[l...m]` and `arr[m + 1...r]`
 */

const merge = (arr) => {

}

const mergesort = () => {

}


/**
 * 
 */
const quicksort = () => {

}


/**
 * 
 */
const insertionsort = () => {

}


module.exports = {
    mergesort,
    quicksort,
    insertionsort,
}